---
name: inkstone
description: 中文优先的静态印刷图表生成 skill。输出矢量 SVG / PDF / 300dpi PNG，无 JavaScript 运行时，可直接置入 Word、LaTeX、InDesign 和幻灯片。按期刊栏宽、中文字号下限、灰度可读性和色盲安全约束出图，所有规范由 scripts/validate.py 机器判定。用于期刊配图、券商研报、教材插图、政企汇报等需要印刷或归档的场合。
---

# Inkstone — 印刷图表规范

给非程序员用。用户给数据和场合，得到一个可以直接拖进 Word / LaTeX / InDesign 的矢量图。

**和屏幕图表库的根本分野**：这里的约束不是审美偏好，是印刷物理。栏宽由版式决定，中文字号有认读下限，0.35pt 以下的线胶印会断，浅于 L*49 的灰印小字看不清。**这些全部由 `scripts/validate.py` 机器判定，不靠自觉。**

---

## 零、硬约束（违反即返工）

1. **必须从 `catalog.md` 锁定图型编号**，再打开 `charts/` 里对应文件，按 `# ════ 编号 图名 ════` 注释块读实现。不得脱离既有图型另写一个"差不多的"。
2. **一切取值来自 `tokens.py`。** 不在代码里写 hex、写 pt、写字体名。与 token 冲突的取值一律以 token 为准。
3. **宽度必须是 `tokens.COLUMN` 里的某一个值。** 图的宽度由载体决定：期刊单栏 85mm、双栏 170mm、A4 正文 156mm。不存在"我觉得这么宽好看"。
4. **中文字号地板 7.5pt，拉丁 6.0pt。** 装不下的信息删掉或换图型，**不许缩小字号硬塞**——屏幕上 6.5px 的中文标签还能勉强认，印出来是一团墨。
5. **默认全灰。** 彩色需要用户明确要求，见第二点五节。
6. **柱长 ∝ 数值，不断轴。** 极端值的三个诚实做法：让它冲天 / 主图加放大镜小图 / 明说画不下并撕柱不撕轴。
   **从零点单向长出来的图（R1 R3 S1 S3 C1 C2）不接受负值，代码强制。** 负值在这些
   几何里没有画法：条会被吞成零长、数值跑到纸外、百格方阵会分出 165 格。有正负走 R2 分岔条。
7. **交付前必须跑 `python3 scripts/validate.py out/*.svg` 且退出码为 0。**

## 一、工作流程

1. **判数据形状。** 别问用户要什么图，看数据长什么样：类目比较？时间序列？构成？分布？多对一归属？形状是选图的主键。
2. **判载体。** 期刊单栏 / 双栏？A4 文档？幻灯片？这一步决定 `column` 参数，也决定能放多少标签——85mm 里塞不下 12 个中文类目名。
3. **在 `catalog.md` 里至少比较 3 个候选，写下淘汰理由。** 只记录"用了哪张"不算数——选择过程本身是交付物的一部分。
4. **按 `charts/` 里的实现出图**，只换数据、标题、副题、来源。
5. **跑校验器**，不合格就返工，不许改校验器去迁就图。
6. **导出。** `scripts/render.py` 一次出 SVG（可再编辑）+ PDF（矢量置入）+ PNG@300dpi（兜底）。

## 二、中文排版硬规则

中文图表翻车集中在这几处，逐条写死：

- **字体栈中西分家。** 拉丁字体在前、CJK 在后，靠 SVG 逐字符 fallback 自动分工。PingFang 自带的拉丁字形又窄又飘，数字尤其难看；直接用 `tokens.FONT_SANS`，别自己拼。
- **标签宽度按实测算，不按字符数估。** 汉字占 1.0em，拉丁约 0.56em——按拉丁经验估宽会算出真实值的一半，标签直接压进图里。用 `charts/_svg.text_width()`。
- **中文类目名不可缩写**（英文的 "Enterprise → Ent." 在中文里没有对应操作）。所以窄栏用**标签在上、条在下**的排布，不要左侧标签栏——否则 85mm 里图形区只剩三分之一。
- **数字用 `tabular-nums`**（`_svg.text()` 已默认开）。等宽数字才能竖向对齐。
- **标题写结论不写图型名。** "协同工具装机量，企业微信仍领先一个身位" 可以；"条形图" 不行。
- **副题承担图例和口径**：单位、时间范围、统计口径写这一行。读者不看代码，只看这一行。

## 二点五、彩色档

**色相可以换，L\* 不能换。** 每套色板都是把 `tokens.GRAY` 的七级明度原样
搬到某个色相上——彩色图影印成黑白后，和 mono 版是同一张。研报和期刊
大量被黑白影印、传真、激光打印，色板不守这条，出版环节就会毁掉它。

```python
with T.use("indigo"):        # indigo 靛青 / ochre 赭石 / celadon 青瓷
    svg = rank_bars(DATA, ...)
```

- **默认 mono。** 用户没明确要求彩色就不要上色。
- **一份交付只用一套色板**，不得混用。
- **不要自己调色。** 用 `palettes.build()` 生成——手调的颜色不会落在
  阶梯明度上，`tests/run.py` 的灰度等价一测就露。要加新色板，在
  `palettes.PRESETS` 里加色相和彩度，明度由 `LADDER` 决定，不是由你决定。
- **色相不承担区分类目的职责。** 单色相 + 明度阶梯本来就不依赖色相辨别，
  这也是它天然色盲安全的原因。要区分类目用直接标注（见 S2 细线族）。
- 色板名会写进 SVG 根元素的 `data-palette`，校验器据此取尺；
  漏了这一步，彩色图的每一处用色都会被拿 mono 灰阶去比。

## 三、卡片四件套

每张图固定四件套，缺一返工：

| 槽位 | 内容 | 字号 |
|---|---|---|
| 图题 | 一句结论 | 10.5pt 五号 |
| 副题 | 口径 · 单位 · 时间范围 | 9pt 小五 |
| 图形 | —— | 标签 7.5pt 六号 |
| 来源行 | 数据来源 · 样本量 | 6.5pt |

## 四、什么时候说不

- **断轴柱状图** → 拒绝，给三个诚实方案。
- **3D、阴影、渐变、发光** → 拒绝。印刷上这些要么变成灰糊，要么增加一次套印。
- **饼图超过 5 片** → 拒绝，换 R1 定序条或构成图。
- **窄栏塞 >8 个中文类目** → 拒绝，改双栏或改图型。
- **要求「每个类目一个颜色」** → 拒绝。色相不承担区分类目的职责，
  改用直接标注或拆图；多色相方案影印后必然糊成一片。
- **要求指定某个具体色值** → 只能作为色相输入交给 `palettes`，
  明度仍由 `LADDER` 决定。原样照搬品牌色会破坏灰度等价。
- **用户没要求彩色** → 不要自作主张上色。
- **数据撑不起所选图型**（3 个点画分布图）→ 降级到更简单的图并说明。
- **空数据** → 拒绝。空图不是图，画一张空白版心比报错糟。

## 五、交付前自检

1. 校验器退出码是 0？
2. 宽度落在 `COLUMN` 表内？
3. 标题是结论不是图型名？副题把口径说清了？
4. 数值和视觉严格成正比？没断轴？面积编码用了 `sqrt`？
5. 打印成黑白后各系列还分得开？（灰阶默认满足，彩色必须实测）
6. 缩到 50% 后最小字还认得出？（期刊常缩排）
7. 写下了 3 个候选和淘汰理由？
8. PDF 里字体已嵌入或转曲，换机器打开不掉字？
9. 如果用了彩色档：整份交付只用一套？`data-palette` 写对了？
   打印成黑白后和 mono 版是不是同一张？

## 六、扩展图型

`catalog.md` 里没有能诚实承载这份数据的图型时，才新造。新图必须：继承 `tokens.py` 的全部取值；写进 `catalog.md`；带 `# ════ 编号 图名 ════` 标记块；通过校验器。
